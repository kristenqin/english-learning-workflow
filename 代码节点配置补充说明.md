# Dify 代码节点配置补充说明

## 问题：输出结果多了一层 `result` 嵌套

### 错误的代码格式 ❌

```python
def main(word_list_text: str) -> dict:
    lines = word_list_text.strip().split('\n')
    words = [line.strip() for line in lines if line.strip()]

    return {
        "result": {  # ❌ 不需要这一层包裹
            "words": words,
            "total_count": len(words)
        }
    }
```

**输出结果**：
```json
{
  "result": {
    "total_count": 30,
    "words": ["time", "person", "go", ...]
  }
}
```

**问题**：后续节点引用变量时需要使用 `解析单词列表.result.words`，增加了复杂度。

---

### 正确的代码格式 ✅

```python
def main(word_list_text: str) -> dict:
    """
    将输入的文本按行分割成单词列表
    """
    lines = word_list_text.strip().split('\n')
    words = [line.strip() for line in lines if line.strip()]

    return {
        "words": words,
        "total_count": len(words)
    }
```

**输出结果**：
```json
{
  "words": ["time", "person", "go", ...],
  "total_count": 30
}
```

**优势**：后续节点可以直接使用 `解析单词列表.words` 引用变量。

---

## 两个代码节点的正确代码

### 代码节点 1：解析单词列表

```python
def main(word_list_text: str) -> dict:
    """
    将输入的文本按行分割成单词列表
    """
    lines = word_list_text.strip().split('\n')
    words = [line.strip() for line in lines if line.strip()]

    return {
        "words": words,
        "total_count": len(words)
    }
```

**输出变量配置**：
- `words` - Array[String]
- `total_count` - Number

**后续引用方式**：
- `解析单词列表.words` → 单词数组
- `解析单词列表.total_count` → 单词总数

---

### 代码节点 2：解析定义和例句

```python
import re

def main(llm_output: str, current_word: str) -> dict:
    """
    解析LLM输出，提取定义和例句列表
    """
    try:
        lines = llm_output.strip().split('\n')

        # 提取定义
        definition = ""
        for line in lines:
            if '—' in line or '–' in line or '-' in line:
                # 提取 "单词 — 定义" 格式
                parts = re.split('[—–-]', line, 1)
                if len(parts) == 2:
                    definition = parts[1].strip()
                    break

        # 如果没找到，尝试其他方式
        if not definition:
            for i, line in enumerate(lines):
                if '定义' in line and i + 1 < len(lines):
                    definition = lines[i + 1].strip()
                    break

        # 提取例句（以 - 或 • 开头的行）
        sentences = []
        for line in lines:
            line = line.strip()
            if line.startswith('-') or line.startswith('•') or line.startswith('*'):
                sentence = line.lstrip('-•* ').strip()
                if sentence:
                    sentences.append(sentence)

        return {
            "definition": definition if definition else "定义提取失败",
            "sentences": sentences,
            "sentence_count": len(sentences)
        }
    except Exception as e:
        return {
            "definition": f"解析错误: {str(e)}",
            "sentences": [],
            "sentence_count": 0
        }
```

**输出变量配置**：
- `definition` - String
- `sentences` - Array[String]
- `sentence_count` - Number

**后续引用方式**：
- `解析定义和例句.definition` → 第一性原理定义
- `解析定义和例句.sentences` → 例句数组
- `解析定义和例句.sentence_count` → 例句数量

---

## Dify 代码节点的返回值规则

### 规则说明

1. **直接返回字典**：`return` 语句直接返回一个字典，字典的键就是输出变量名
2. **键名即变量名**：字典的键名要与"输出变量"配置中的变量名一致
3. **不需要额外包裹**：不需要用 `result` 或其他名称包裹一层

### 示例对比

| 返回格式 | 引用方式 | 是否推荐 |
|---------|---------|---------|
| `{"words": [...]}` | `节点.words` | ✅ 推荐 |
| `{"result": {"words": [...]}}` | `节点.result.words` | ❌ 不推荐 |

---

## 如何修复已有的代码节点

### 方案 1：修改代码（推荐）

1. 打开代码节点
2. 删除 `return` 语句中的 `"result": {` 和对应的 `}`
3. 保存节点
4. **不需要修改输出变量配置**

### 方案 2：保持代码不变，调整后续引用

如果你已经搭建了很多后续节点，不想修改代码，可以：

1. 保持代码中的 `"result"` 包裹
2. 在后续节点引用时使用：`解析单词列表.result.words`
3. 在迭代节点的输入变量中选择：`解析单词列表.result.words`

**注意**：方案 2 不推荐，会让工作流配置变得复杂。

---

## 测试输出结果

### 正确的输出（无 result 层）

```json
{
  "words": [
    "time",
    "person",
    "go",
    "see",
    "make",
    "good",
    "new",
    "day",
    "work",
    "take",
    "get",
    "come",
    "think",
    "look",
    "use",
    "find",
    "give",
    "tell",
    "many",
    "long",
    "first",
    "want",
    "know",
    "because",
    "over",
    "little",
    "great",
    "help",
    "call",
    "right"
  ],
  "total_count": 30
}
```

### 如何验证

1. 运行代码节点
2. 点击节点查看输出
3. 检查 JSON 结构是否**直接包含** `words` 和 `total_count`
4. 确认没有额外的 `result` 包裹层

---

## 其他可能的优化

### 1. 添加数据验证

```python
def main(word_list_text: str) -> dict:
    """
    将输入的文本按行分割成单词列表，并进行验证
    """
    if not word_list_text or not word_list_text.strip():
        return {
            "words": [],
            "total_count": 0,
            "error": "输入文本为空"
        }

    lines = word_list_text.strip().split('\n')
    words = [line.strip() for line in lines if line.strip()]

    # 过滤掉非字母开头的行（可能是注释或标题）
    words = [w for w in words if w and w[0].isalpha()]

    return {
        "words": words,
        "total_count": len(words)
    }
```

### 2. 添加去重功能

```python
def main(word_list_text: str) -> dict:
    """
    将输入的文本按行分割成单词列表，并去重
    """
    lines = word_list_text.strip().split('\n')
    words = [line.strip().lower() for line in lines if line.strip()]

    # 去重，保持顺序
    unique_words = []
    seen = set()
    for word in words:
        if word not in seen:
            unique_words.append(word)
            seen.add(word)

    return {
        "words": unique_words,
        "total_count": len(unique_words),
        "duplicate_count": len(words) - len(unique_words)
    }
```

### 3. 添加单词过滤

```python
def main(word_list_text: str) -> dict:
    """
    将输入的文本按行分割成单词列表，过滤无效单词
    """
    lines = word_list_text.strip().split('\n')

    words = []
    for line in lines:
        word = line.strip()
        # 过滤条件：
        # 1. 不为空
        # 2. 只包含字母（允许连字符）
        # 3. 长度在 2-20 之间
        if word and len(word) >= 2 and len(word) <= 20:
            if all(c.isalpha() or c == '-' for c in word):
                words.append(word.lower())

    return {
        "words": words,
        "total_count": len(words)
    }
```

---

## 总结

✅ **关键要点**：
1. Dify 代码节点的 `return` 语句直接返回字典，不需要 `result` 包裹
2. 字典的键名要与输出变量配置一致
3. 后续节点引用时使用 `节点名.变量名`

✅ **你需要做的**：
1. 修改代码节点，移除 `"result": {` 这一层
2. 保存节点
3. 测试输出格式是否正确
4. 继续配置后续的迭代节点

如有问题随时提问！
